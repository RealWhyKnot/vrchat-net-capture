[tool.ruff]
target-version = "py310"
line-length = 120
exclude = [
    ".git",
    ".mypy_cache",
    ".pytest_cache",
    ".ruff_cache",
    ".venv",
    "captures",
    "dist",
    "bin",
    "obj",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
